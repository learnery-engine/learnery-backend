version: '3.8'
services:
  app:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '8080:${PORT}'
    environment:
      - NODE_ENV=production
#    volumes:
#      - ./temp:/app
